#include<iostream>
#include<iomanip>
using namespace std;

int main()
{
    /*-----------------For Loop----------------------------------
        Syntax
        for(initialisation; condition; updation)
        {
            Loop body(C++ code);
        }
    */
   //Code for table of 6
   //int i,count;
   //for(i=0;i<=10;i++)
   //{
   //    count=6*i;
   //    cout<<"6 * "<<i<<"="<<count<<endl;
   //}

   /*--------------------While Loop----------------------------------
        Syntax
        while(condition)
        {
            Loop body(C++ code);
        }

   */

    //int i=0,n;
    //while(i<5)
    //{
    //    cout<<setw(4)<<i;
    //    i++;
    //}

    /*------------------Do-While Loop-------------------
        Syntax 
        do
        {
            Loop body(C++ code);
        }while(condition);
    */

    //int i=10000;
    //do
    //{
    //    cout<<"The value of i is "<<setw(5)<<i<<endl;
    //    i++;
    //}while(i<=99999);

    /*
    Loops can also be infinite and can cause dammage to the computer system as the 
    consume a lot more memory.
    */
    return 0;
}